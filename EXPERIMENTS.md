# 实验记录与边界

日期：2026-09-24。以下数字整理自维护者在独立 H800 训练服务器上运行并提供的日志；未分发模型权重及完整原始训练运行目录。它们是开发阶段单次实验，不是多随机种子统计结论。

## 模型与任务

- Qwen3.5-0.8B（非 Base），文本主干全参数训练，新增 18 类动作头。
- 输入为当前完整魔方状态；不输入打乱过程、求解器答案或距离。
- 已验证 GPU 环境为 Python 3.11 / torch 2.6.0+cu124 / transformers 5.3.0。
- 单张 H800 上监督训练观测峰值显存约 14.08 GiB，GRPO pilot 约 16.88 GiB；具体需求受批大小和环境影响。

## 固定浅层验证

使用 `cube_trajectories_expanded.jsonl`、seed 17、max_steps 10 重建 332 个起点（76 个精确二步、256 个精确三步）。推理为贪心 argmax，不加动作过滤或搜索。所有半转也计一步。

| 模型 | 总成功数 | 二步成功数 | 三步成功数 | 重复状态比例 |
|---|---:|---:|---:|---:|
| SFT 256 | 132/332 | 52/76 | 80/256 | 58.13% |
| SFT 323 | 185/332 | 58/76 | 127/256 | 43.67% |
| SFT 1000 | 222/332 | 73/76 | 149/256 | 32.53% |
| SFT 1000 + GRPO 20 轮 | 224/332 | 73/76 | 151/256 | 31.93% |

不同 SFT 实验的训练样本数、停止轮数和更新次数不同。GRPO 还扩展了在线训练起点，因此不是相同数据量下的优化器对比。验证起点未进入对应训练数据，但后续状态可能出现过；未排除旋转等价状态。重复使用该集合意味着它是开发验证集，而不是未使用过的最终测试集。

GRPO pilot 共尝试 80 组，保留 63 组、504 条轨迹，丢弃 17 组。只有 6 个保留组有成功与失败的差异；20 轮中有 15 轮没有直接的组内奖励优势。Brier 辅助损失仍会更新共享主干，因此净增加两个成功案例不能明确归因于 GRPO。成功概率头未进行独立校准评估。

## 探索诊断：冻结 SFT 1000 模型

相同的 256 个非验证起点，seed 29，每组 8 条、最多 10 步。没有权重更新。

| 温度 | 有效组 | 丢弃组 | 每千动作有效组 |
|---|---:|---:|---:|
| 1.0 | 11 | 55 | 1.174 |
| 1.6 | 21 | 65 | 2.263 |
| 2.0 | 30 | 70 | 3.232 |
| 2.5 | 35 | 76 | 3.745 |

温度 2.5 在本次探测中增加了有效信号，但不能据此声称已经提高贪心还原率。保留组分布随温度变化，采样成功率不能作为统一起点集上的能力对比。

## 尚未完成

- 从专家后缀与逆向随机游走构建渐进式课程。
- 高温采样与训练损失一致的 GRPO、有效组补采样。
- 失败状态的专家纠正数据聚合。
- 多随机种子及独立随机三阶魔方评估。
- 将本地训练模型接入网页，或加入有固定预算的搜索对照。

现有 1000 条随机魔方求解器轨迹只是已验证的原始数据，不能作为模型已能还原随机魔方的证据。
