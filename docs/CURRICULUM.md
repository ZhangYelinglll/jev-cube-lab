# 课程数据 v1：专家后缀

这是下一阶段的数据准备，不是课程训练完成的结果。基于已发布的 1000 条完整轨迹，以种子 20260924 按轨迹分为 800 / 100 / 100 条训练、验证、最终测试来源。先划分再展开，避免同一原始轨迹被直接拆进不同集合。

## 生成与检查

在项目根目录运行，不需要 GPU、PyTorch 或 API：

```bash
python -m cube.curriculum
python -m tests.check_curriculum
```

默认输出 `data/curriculum_v1/`，已存在时拒绝覆盖。重建时使用新目录：

```bash
python -m cube.curriculum --output data/curriculum_rebuilt
```

## 数据隔离

- 原 332 个浅层验证起点继续保留。训练候选轨迹若经过任一预留起点，整条排除，避免未来展开其后缀时泄漏。
- 因此 800 条候选训练轨迹中排除 275 条，实际保留 525 条。原始轨迹仍保存在原始数据文件，不移动到其他划分。
- 新验证和最终测试起点均排除当前全部课程训练状态、之前 SFT 1000 个状态，以及旧的 332 个预留起点。
- 最终测试起点还排除验证来源轨迹里的全部非终局状态。
- 每个集合内部按状态去重，选择该集合里观察到的最短专家后缀，并保留来源 ID 和步骤。
- 这是精确状态级的起点隔离，后续还原状态允许重叠，没有排除旋转等价状态。不同后缀可能来自同一原始轨迹，不能把所有评估起点当作相互独立的随机魔方。
- 最终测试数据仅准备，尚未运行模型评估。后续扩大训练数据时需要重新检查与保留测试起点的重叠。

## 本批实际数量

| 集合 | 去重后的状态数 |
|---|---:|
| 训练 | 9969 |
| 验证 | 1879 |
| 最终测试 | 1863 |

| 专家剩余步数 | 训练 | 验证 | 最终测试 |
|---|---:|---:|---:|
| 1 | 9 | 0 | 0 |
| 2 | 44 | 0 | 0 |
| 3 | 222 | 26 | 20 |
| 4 | 436 | 77 | 78 |
| 5 | 509 | 94 | 96 |

一、二步验证起点为零是排除重叠后的真实结果，不能为了凑数放回训练中出现的状态。一阶段仍需使用原有浅层验证口径。训练后缀最后一步只覆盖 9 种状态，也说明求解器示范存在覆盖偏差；后续需要混合原有数据与从终点出发的随机游走，不能仅沿这 525 条路径推进难度。

## 输出文件与字段

- `train.jsonl`、`validation.jsonl`、`test.jsonl`：分组后的逐步样本，每行一个状态及其可行后缀。
- `train_upto_3.jsonl`：专家剩余不超过 3 步，共 275 个状态。
- `train_upto_5.jsonl`：专家剩余不超过 5 步，共 1220 个状态。
- `manifest.json`：来源轨迹划分及保留的训练轨迹 ID。
- `report.json`：每层数量、过滤数量和实验边界。

`expert_remaining` 是选择的可行后缀长度，**不代表最短距离**。`optimal_distance` 仅在现有深度 3 的精确 BFS 表覆盖该状态时填写，否则为 null。`solution` / `target_action` / `target_index` 是监督标签，不能作为策略输入。`source_id` / `source_step` 用于追踪来源。

目前阶段文件没有自动混入旧 SFT 数据，也没有新增随机游走样本。`cube.train --curriculum` 已支持这一格式，并逐条回放验证专家解法。`cube.eval` 仍面向浅层验证，不能直接用新课程验证文件评估。

## 下一步

监督训练已支持新格式；接下来实现课程闭环评估，保持真实还原率作为主要指标；加入旧浅层状态回放和多样化随机游走数据，并重新校验数据隔离。先完成 1～3 步、4～5 步的监督基线，再比较同起始模型上的课程 GRPO。新的阶段长度应明确标为专家剩余步数；不能将 4～5 步后缀的结果宣称为精确四、五步距离的结果。

## 开始第一阶段课程监督训练

从原来的 SFT 1000 检查点继续，混入原有状态；重叠状态保留原来的标签。检查点恢复主干与动作头，优化器重新初始化。`--curriculum` 按专家剩余步数报告标签准确率，不把它误称为最短距离。

```bash
git pull --ff-only
CUDA_VISIBLE_DEVICES=1 python -m cube.train \
  --checkpoint "runs/overfit-1000/checkpoint" \
  --curriculum \
  --data data/curriculum_v1/train_upto_5.jsonl \
  --replay-data data/cube_trajectories_1000.jsonl \
  --output "runs/curriculum-sft-5-v1" \
  --batch-size 8 --epochs 20 \
  --backbone-lr 1e-6 --head-lr 1e-5
```

所有命令在项目根目录执行，输出保存在 `runs/`，该目录由 `.gitignore` 排除。若已有检查点保存在其他位置，将 `--checkpoint` 改为它的实际路径，无需重新训练或移动。输出目录必须未存在。训练最多 20 轮，训练标签准确率达到默认 99% 时提前停止。此阶段属于监督学习，不是 GRPO；课程验证起点尚未参与训练，也没有基于它们选择最佳检查点。训练日志中的标签准确率不是完整还原率。
